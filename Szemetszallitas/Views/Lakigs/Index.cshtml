@model IEnumerable<Szemetszallitas.Models.Lakig>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.igeny)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Szolgaltatas!.tipus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.mennyiseg)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.igeny)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Szolgaltatas!.tipus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.mennyiseg)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
